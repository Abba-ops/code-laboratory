<nav class="navbar navbar-expand-lg bg-white fixed-top border-bottom">
  <div class="container">
    <a class="navbar-brand" href="/">Finding Grace</a>
    <button
      type="button"
      class="navbar-toggler"
      data-bs-target="#navbar"
      data-bs-toggle="collapse"
      aria-expanded="false">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-center" id="navbar">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link <%= url === "/blogs" ? "active" : "" %>" href="/">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= url === "/about" ? "active" : "" %>" href="/about">About Us</a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= url === "/blogs/create" ? "active" : "" %>" href="/blogs/create">Create Blog</a>
        </li>
      </ul>
      <% if (!user) { %>
      <div class="navbar-nav d-flex flex-row gap-3 my-3 d-lg-none">
        <li class="nav-item">
          <a class="btn btn-outline-primary" href="/login">Sign In</a>
        </li>
        <li class="nav-item">
          <a class="btn btn-primary" href="/signup">Sign Up</a>
        </li>
      </div>
      <% } else { %>
      <div class="navbar-nav d-flex flex-row gap-3 my-3 d-lg-none">
        <li class="nav-item">
          <a class="btn btn-danger" href="/logout">Log Out</a>
        </li>
      </div>
      <% } %>
    </div>
    <div class="navbar-nav d-none d-lg-flex gap-3">
      <% if (!user) { %>
      <li class="nav-item">
        <a class="btn btn-outline-primary" href="/login">Sign In</a>
      </li>
      <li class="nav-item">
        <a class="btn btn-primary" href="/signup">Sign Up</a>
      </li>
      <% } else { %>
      <li class="nav-item">
        <a class="btn btn-danger" href="/logout">Log Out</a>
      </li>
      <% } %>
    </div>
  </div>
</nav>
